# PRODUCTION ENVIRONMENT VARIABLES FOR VERCEL
# Copy these to your Vercel Dashboard > Settings > Environment Variables

# ============================================
# DATABASE - REQUIRED
# ============================================
# Use connection pooling URL (ends with -pooler or ?pgbouncer=true)
DATABASE_URL="postgresql://user:password@host-pooler.aws.neon.tech/dbname?sslmode=require"

# Direct database URL (without pooling) for migrations
# Use the same URL but without -pooler or pgbouncer
DIRECT_DATABASE_URL="postgresql://user:password@host.aws.neon.tech/dbname?sslmode=require"

# ============================================
# SECURITY - REQUIRED
# ============================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-32-characters-or-more"
CSRF_SECRET="your-super-secret-csrf-key-32-characters-or-more"

# ============================================
# APP CONFIGURATION
# ============================================
NODE_ENV="production"
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

# ============================================
# OPTIONAL - For development features
# ============================================
# SKIP_ENV_VALIDATION="true"
